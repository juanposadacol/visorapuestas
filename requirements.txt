# Dependencias de ejecucion. Todo local, gratuito y sin suscripciones.
# Instalar con:  pip install -r requirements.txt

PySide6-Essentials>=6.6          # interfaz grafica
mss>=9.0                         # captura de pantalla
opencv-python-headless>=4.9      # preprocesado de imagen para el OCR
numpy>=1.24
rapidocr-onnxruntime>=1.3        # motor OCR local (modelos incluidos en el paquete)
pynput>=1.7                      # atajos de teclado globales (F8/F9/F10/F11)

# --- Opcionales -----------------------------------------------------------
# Motor OCR alternativo; ademas hay que instalar el programa Tesseract:
# pytesseract>=0.3.10
# Pillow>=10.0
#
# Captura mas rapida en Windows (puede fallar con algunos drivers):
# dxcam>=0.0.5
